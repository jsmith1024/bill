# forloop.bill
# for loop syntax sample

fun noval main(argv):
{
    for(var int32 i = 0; i < 1; i += .01):
    {
        writeln(i);
    }

    # foreach (sequence types only)
    foreach(var str arg, argv):
    {
        writeln(arg);
    }
}

